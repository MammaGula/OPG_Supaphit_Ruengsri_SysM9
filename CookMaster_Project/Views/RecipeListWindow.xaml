<Window x:Class="CookMaster_Project.Views.RecipeListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Recipe List" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="10">
            <TextBlock Text="{Binding LoggedInUsername}" FontSize="16" Margin="0,0,10,0"/>
            <Button Content="User" Command="{Binding OpenUserDetailsCommand}" Margin="5,0"/>
            <Button Content="Sign Out" Command="{Binding SignOutCommand}" Margin="5,0"/>
        </StackPanel>

        <!-- Recipe List -->
        <ListView Grid.Row="1"  Margin="10">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Title"  Width="200"/>
                    <GridViewColumn Header="Date"  Width="150"/>
                </GridView>
            </ListView.View>
        </ListView>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="10">
            <Button Content="Add Recipe"  Margin="5,0"/>
            <Button Content="Details"  Margin="5,0"/>
            <Button Content="Remove"  Margin="5,0"/>
            <Button Content="Info"  Margin="5,0"/>
        </StackPanel>
    </Grid>
</Window>
